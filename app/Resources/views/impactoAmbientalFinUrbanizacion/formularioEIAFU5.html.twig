{% extends 'default/baseFormularios.html.twig' %}

{% block titleForm %} FORMULARIO DE ESTUDIO DE IMPACTO AMBIENTAL PARA LOTEOS CON FINES DE URBANIZACIÓN {% endblock %}

{% block subTitle %} 5. DATOS DE BASE {% endblock %}

{% block contenctForms %}
<div class="card-body">
<form action="{{ path('tramiteEIAFU5') }}" method="post">
    <input type="hidden" name="idTramite" value="{{idTramite}}">
    {% set i = 1 %}
    <h6>5.1 - Caracterización climática (Humedad, temperatura, viento, precipitaciones). <span class="text-danger">(*)</span></h6>
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage1 is defined and Storage1 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage1.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>
    {% set i = i + 1 %}
    <h6>5.2 - Presentación de la "rosa de los vientos" predominantes. <span class="text-danger">(*)</span></h6>
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage2 is defined and Storage2 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage2.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.3 - Análisis representativo de la calidad de agua para consumo humano. <span class="text-danger">(*)</span></h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage3 is defined and Storage3 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage3.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.4 - Análisis de calidad de aire. <span class="text-danger">(*)</span></h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage4 is defined and Storage4 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage4.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.5 - Estudio de ruido, solo en caso de corresponder. </h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage5 is defined and Storage5 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage5.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.6 - Relevamiento de la flora, fauna y recursos abióticos. <span class="text-danger">(*)</span></h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage6 is defined and Storage6 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage6.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.7 - Relevamiento de patrimonio arqueológico, de corresponder. </h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage7 is defined and Storage7 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage7.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.8 - Identificar población con necesidad de relocalización. <span class="text-danger">(*)</span></h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage8 is defined and Storage8 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage8.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <hr>

    <h6>5.9 - Declaración de existencia de pasivos ambietales o sitios contaminados. <span class="text-danger">(*)</span></h6>
    {% set i = i + 1 %}
    <div class="row">
        <div class="col-md-6">
            <input id="archivo{{i}}" class="p-top mb-1" type="file" accept=".pdf" /> 
            <a id="button{{i}}" onclick="subirArchivo({{i}},'5.{{i}}')"  class="btn btn-info text-light">Subir</a>
        </div>
        <div id="nombreArchivo{{i}}" class="col-md-6 p-top">
            {% if Storage9 is defined and Storage9 != null %}
                <div>
                    <h6 class="text-success"><i class='fas fa-check'></i> {{Storage9.nombre}} </h6>
                </div>
            {% endif %}
            <div id="spinner{{i}}" class='spinner form-row'>
                <div class="three-quarters-loader"></div>
            </div>
        </div>
    </div> 
    <br/>
    
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script src="{{asset('bundles/js/archivoIndividual.js')}}"></script>
{% endblock %}